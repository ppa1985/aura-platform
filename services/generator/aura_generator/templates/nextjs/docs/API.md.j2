# API Reference — {{ bp.name }}

> Auto-generated by Aura. Every entity in the Blueprint gets a REST-ish CRUD
> surface under `/api/<entity-kebab>`. Requests/responses are JSON.

## Conventions

- Base URL: the app's origin (e.g. `http://localhost:3000` in dev,
  `/generated/{{ bp.slug }}` behind the platform proxy).
- Content type: `application/json` on both sides.
- IDs are UUIDs.
- Timestamps are ISO-8601 strings (`created_at`, `updated_at`).
- Errors: `4xx` with `{"error": "<message>"}`; `5xx` for unexpected failures.

## System fields (present on every record)

| Field | Type | Notes |
|-------|------|-------|
| `id` | string (uuid) | Server-assigned on create. |
| `created_at` | string (datetime) | Set on insert. |
| `updated_at` | string (datetime) | Refreshed on update. |

## Endpoints

{% for e in entities %}
### {{ e.name }}

Base path: `/api/{{ e.slug }}`

**Schema**

| Field | Type | Required | Unique |
|-------|------|----------|--------|
{% for f in e.fields -%}
| `{{ f.name }}` | {{ f.type }} | {{ "yes" if f.required else "no" }} | {{ "yes" if f.unique else "no" }} |
{% endfor %}
{%- if e.relations %}

**Relations**

{% for r in e.relations -%}
- `{{ r.column }}` → {{ r.target }}.`id` ({{ "required" if r.required else "optional" }})
{% endfor %}
{%- endif %}

**Operations**

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/api/{{ e.slug }}` | List all {{ e.name }} records (newest first). |
| `POST` | `/api/{{ e.slug }}` | Create a {{ e.name }}. |
| `GET` | `/api/{{ e.slug }}/{id}` | Fetch one {{ e.name }} by id. |
| `PUT` | `/api/{{ e.slug }}/{id}` | Replace a {{ e.name }}. |
| `DELETE` | `/api/{{ e.slug }}/{id}` | Delete a {{ e.name }}. |

**Example — create**

```bash
curl -X POST http://localhost:3000/api/{{ e.slug }} \
  -H 'content-type: application/json' \
  -d '{
{%- for f in e.fields %}
  "{{ f.name }}": {% if f.type in ["integer","float","decimal"] %}0{% elif f.type == "boolean" %}false{% else %}"example"{% endif %}{% if not loop.last or e.relations %},{% endif %}
{%- endfor %}
{%- for r in e.relations %}
  "{{ r.column }}": "<{{ r.target|lower }}-uuid>"{% if not loop.last %},{% endif %}
{%- endfor %}
}'
```
{% endfor %}
{%- if bp.ai_enabled %}

## AI

Base path: `/api/ai`

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/api/ai` | Send a natural-language request; returns model output. |

Request body: `{"prompt": "..."}`. Configured provider comes from the Blueprint
(`ai_config.provider`, `ai_config.model`).
{%- endif %}
