# {{ bp.name }}

{{ bp.description }}

Generated by **Aura** — the autonomous app generation platform.

## Entities

{% for e in entities -%}
- **{{ e.name }}** — {{ e.fields|length }} fields, {{ e.relations|length }} relations
{% endfor %}

## Run locally

```bash
npm install
AURA_BASE_PATH=/ DATABASE_URL=postgres://... npm run dev
```

## AI

AI integration is {{ "enabled" if bp.ai_enabled else "disabled" }} for this app.
